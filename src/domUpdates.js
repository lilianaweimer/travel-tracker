const domUpdates = {
    user: undefined,

    attemptLogin() {
        let usernameInput = document.querySelector();
        let passwordInput = document.querySelector();
        if (usernameInput === 'traveler45' && passwordInput === 'travel2020') {
            user = new this.user()
        } else if (usernameInput === 'agency' && passwordInput === 'travel2020') {

        }
    },

}

module.exports = domUpdates;